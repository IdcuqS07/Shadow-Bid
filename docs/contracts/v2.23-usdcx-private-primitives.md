# ShadowBid V2.23 USDCx Private Primitives

## Status

Generated from the local imported stablecoin instructions at `contracts/build/imports/test_usdcx_stablecoin.aleo`.

Last generated: `2026-04-03T16:32:07.396Z`

Current planning note: the primitives exist, but the `USDCx` private-first path is paused because the repo cannot self-source a usable `get_credentials` proof.

## Why This Exists

`v2.23` needs a private `USDCx` funding rail that avoids public amount leakage.

This document captures the concrete primitives already available in the imported `test_usdcx_stablecoin.aleo` program so the team can design the spike around real rails instead of assumptions.

## Key Discovery

The local import already exposes the primitives we would need to explore a hidden-amount rail:

- `get_credentials`
- `transfer_private`
- `transfer_private_with_creds`
- `transfer_private_to_public`

The intended hidden-amount path is:

1. obtain a `Credentials` record with `get_credentials`
2. move a private `Token` record with `transfer_private_with_creds`
3. avoid `transfer_public_to_private` and `transfer_private_to_public` for the commit path because they expose the amount publicly

## Record Types

```text
record Token:
    owner as address.private;
    amount as u128.private;

record ComplianceRecord:
    owner as address.private;
    amount as u128.private;
    sender as address.private;
    recipient as address.private;

record Credentials:
    owner as address.private;
    freeze_list_root as field.private;

struct MerkleProof:
    siblings as [field; 16u32];
    leaf_index as u32;
```

## Function Map

### get_credentials

- Classification: `private-credentials`
- Hidden-amount candidate: `true`
- Public amount leak: `false`
- Inputs:
  - r0 as [MerkleProof; 2u32].private
- Outputs:
  - r410 as Credentials.record
  - r411 as test_usdcx_stablecoin.aleo/get_credentials.future
- Notes:
  - Produces a Credentials record tied to the current freeze-list root.
  - Requires private Merkle proofs as input.
  - Needed before using transfer_private_with_creds.
  - Contract availability does not mean the repo can operationally produce its proof inputs.

### mint_private

- Classification: `private-seeding`
- Hidden-amount candidate: `true`
- Public amount leak: `false`
- Inputs:
  - r0 as address.private
  - r1 as u128.public
- Outputs:
  - r3 as ComplianceRecord.record
  - r2 as Token.record
  - r4 as test_usdcx_stablecoin.aleo/mint_private.future
- Notes:
  - Useful for controlled local/test seeding only.
  - Requires a minter role in the stablecoin program.
  - Not a user-facing auction funding primitive.

### transfer_public_to_private

- Classification: `public-to-private`
- Hidden-amount candidate: `false`
- Public amount leak: `true`
- Inputs:
  - r0 as address.private
  - r1 as u128.public
- Outputs:
  - r3 as ComplianceRecord.record
  - r2 as Token.record
  - r4 as test_usdcx_stablecoin.aleo/transfer_public_to_private.future
- Notes:
  - Creates a private Token record from a public transfer.
  - Amount is public at transition time, so it does not satisfy hidden-amount bidding.

### transfer_private

- Classification: `private-transfer`
- Hidden-amount candidate: `true`
- Public amount leak: `false`
- Inputs:
  - r0 as address.private
  - r1 as u128.private
  - r2 as Token.record
  - r3 as [MerkleProof; 2u32].private
- Outputs:
  - r416 as ComplianceRecord.record
  - r414 as Token.record
  - r415 as Token.record
  - r417 as test_usdcx_stablecoin.aleo/transfer_private.future
- Notes:
  - Moves a private Token record using Merkle proofs.
  - Candidate primitive when credentials are not required or not refreshed.

### transfer_private_to_public

- Classification: `private-to-public`
- Hidden-amount candidate: `false`
- Public amount leak: `true`
- Inputs:
  - r0 as address.public
  - r1 as u128.public
  - r2 as Token.record
  - r3 as [MerkleProof; 2u32].private
- Outputs:
  - r415 as ComplianceRecord.record
  - r414 as Token.record
  - r416 as test_usdcx_stablecoin.aleo/transfer_private_to_public.future
- Notes:
  - Useful as an exit rail or fallback, not as the hidden-amount commit path.
  - Public recipient and public amount leak the transfer amount by design.

### transfer_private_with_creds

- Classification: `private-transfer-with-creds`
- Hidden-amount candidate: `true`
- Public amount leak: `false`
- Inputs:
  - r0 as address.private
  - r1 as u128.private
  - r2 as Token.record
  - r3 as Credentials.record
- Outputs:
  - r7 as ComplianceRecord.record
  - r5 as Token.record
  - r6 as Token.record
  - r8 as Credentials.record
  - r9 as test_usdcx_stablecoin.aleo/transfer_private_with_creds.future
- Notes:
  - Strongest candidate primitive for a private USDCx bid funding rail.
  - Consumes a private Token record and a Credentials record.
  - Returns change, recipient Token, refreshed Credentials, and a ComplianceRecord.


## Current Integration State

The local probe wrapper now compiles with the shortened program id `shadowbid_usdcx_probe_v1.aleo`.

Practical implication:

- the private rail is present and callable from a local probe package
- the next blocker is no longer parser visibility
- the next blocker is sourcing a real external proof source for `get_credentials`, plus any required private `Token` records or `Credentials` records for an end-to-end spike

## Recommended Next Step

Keep `transfer_private_with_creds` and `get_credentials` documented as the relevant private rail primitives, but do not schedule another real funding spike on this path. Only reopen the `USDCx` private-first experiment if an external proof source can provide the missing `get_credentials` inputs or a usable `Credentials` record.
