# ShadowBid V2.23 USDCx Preconditions

## Status

Generated from live `testnet` mapping queries against `https://api.explorer.provable.com/v1/testnet`.

Last generated: `2026-04-03T16:32:26.183Z`

Target address: `aleo1lne9r7laz8r9pwmulkseuvfyem7h9f2hcelgm0me4a708h3avv8qz8ggz8`

## Why This Exists

`v2.23` needs more than a primitive map.

Before reopening a real `get_credentials -> transfer_private_with_creds` spike, the repo needs to know whether the live stablecoin state actually permits the experiment and whether the chosen wallet has a plausible proof source instead of only a plausible private funding source.

## Snapshot

| Query | Value | Interpretation |
| --- | --- | --- |
| `pause[true]` | `false` | Private transfer finalizers are not paused. |
| `freeze_list_root[1u8]` | `3642222252059314292809609689035560016959342421640560347114299934615987159853field` | Primary freeze-list root is present for `get_credentials` and credentialed transfers. |
| `freeze_list_root[2u8]` | `null` | No fallback root is currently staged. |
| `block_height_window[true]` | `300u32` | Window used when a fallback root is active. |
| `root_updated_height[true]` | `null` | No staged-root update height is recorded right now. |
| `address_to_role[address]` | `null` | Address does not have a stablecoin admin/minter role. |
| `freeze_list[address]` | `null` | Address is not explicitly frozen; the stablecoin finalizers treat missing entries as `false`. |
| `balances[address]` | `20993000u128` | A public balance exists, but converting it to private would leak the amount. |

## Assessment

- `get_credentials`: `contract-present-but-proof-source-blocked`
- `transfer_private_with_creds`: `contract-present-but-blocked-by-credentials`
- Hidden-amount funding source: `no-viable-proof-source`
- Current blocker: the live contract state permits the transition family, but the repo still cannot source a usable `get_credentials` proof payload from public mappings alone.

## Recommendation

- Keep `transfer_private_with_creds` documented as the target primitive, but do not treat it as an active next spike.
- Do not treat a visible public balance as evidence that full hidden-amount funding is solved.
- Do not spend more time on repo-only proof reconstruction from public mappings.
- Only reopen this rail if an external proof source can provide raw sibling fields or a usable `Credentials` record.

## Related Documents

- `docs/contracts/v2.23-usdcx-private-primitives.md`
- `docs/contracts/v2.23-benchmark-findings.md`
- `docs/contracts/v2.23-phase-1-feasibility-spike.md`
