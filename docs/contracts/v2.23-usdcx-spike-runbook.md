# ShadowBid V2.23 USDCx Spike Runbook

## Status

Generated for the current best bidder-test candidate wallet.

Last generated: `2026-04-03T16:52:45.330Z`

This runbook is now retained as a historical reference. Execution is paused because the repo cannot currently produce the `get_creds` proof array from public mappings alone.

## Scope

This runbook was for a narrow rail-validation spike only.

It does not prove final escrow custody. It only describes how the repo would attempt `get_creds -> xfer_with_creds` if an external proof source became available.

## Wallets

- Sender wallet: `aleo1ecrqqyvaszpehqc967g5aau4uqt2dg3y5ardm0y5wf4hxjajzsyqm5cxke`
- Recipient wallet: `aleo1lne9r7laz8r9pwmulkseuvfyem7h9f2hcelgm0me4a708h3avv8qz8ggz8`
- Target amount: `20000u128` (`0.02 USDCx`)
- Bootstrap tx: [`at1yez8a5zetyfzjf0pfh43zef0zjndk4hjwa9z32mza8t4q6qj8s8sz86uj6`](https://testnet.explorer.provable.com/transaction/at1yez8a5zetyfzjf0pfh43zef0zjndk4hjwa9z32mza8t4q6qj8s8sz86uj6)

## Why This Wallet

- The sender wallet has a known bootstrap transaction that called `transfer_public_to_private` for `4500000u128`.
- The sender wallet also shows a public balance of `4500000u128` in the latest candidate precondition snapshot.
- If the wallet UI still shows `4.5 USDCx Private`, this is currently the strongest practical candidate for the next private rail test.

## Assumptions

- The sender wallet still holds an unspent private `Token.record`.
- The recipient wallet is only a temporary sink for rail validation.
- This test should be described as `private transfer rail validation`, not `full privacy proven`.

## Steps

Do not execute these steps unless the external proof-source blocker is resolved.

1. Confirm in the wallet UI that the sender still shows `4.5 USDCx Private` as spendable.
2. Register the probe target:
   `cd contracts/programs/shadowbid_usdcx_private_probe_v1 && leo run register_probe_target 36045056131843534541774283033field 1u8 true true false 20000u128 1775235165i64`
3. Prepare the proof array and request credentials:
   `cd contracts/programs/shadowbid_usdcx_private_probe_v1 && leo run get_creds <proof_array_private>`
4. Transfer privately with credentials:
   `cd contracts/programs/shadowbid_usdcx_private_probe_v1 && leo run xfer_with_creds aleo1lne9r7laz8r9pwmulkseuvfyem7h9f2hcelgm0me4a708h3avv8qz8ggz8 20000u128 <private_token_record> <credentials_record>`
5. Record the tx id and inspect the explorer/API output for public amount leakage.
   Example:
   `npm run v223:visibility -- --tx <private_transfer_tx_id> --expected-amount 20000 --compare-tx at1yez8a5zetyfzjf0pfh43zef0zjndk4hjwa9z32mza8t4q6qj8s8sz86uj6`
6. Close the probe metadata:
   `cd contracts/programs/shadowbid_usdcx_private_probe_v1 && leo run close_probe_target 36045056131843534541774283033field 1775238765i64`

## Leak Check Checklist

- The private transfer transaction should not expose `20000u128` as a public input.
- The transaction should not resemble the bootstrap `transfer_public_to_private` shape, which exposed `4500000u128` publicly.
- Public balance mappings for sender and recipient should not become the evidence source for the private transfer amount.

## Go Or No-Go

- Current verdict: `no-go`
- Reopen only if the sender wallet, a vendor API, an issuer flow, or an off-chain proof builder can surface the proof array needed by `get_creds`.
- The historical go condition remains: the sender wallet still shows the private balance and can surface the private record and proof inputs.

## Related Documents

- `docs/contracts/v2.23-usdcx-preconditions-candidate.md`
- `docs/contracts/v2.23-usdcx-funding-paths.md`
- `docs/contracts/v2.23-benchmark-findings.md`
